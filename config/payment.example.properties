# Copy to config/payment.properties and enter the values issued by VNPAY Sandbox.
# Keep payment.properties out of Git. Environment variables also remain supported.
app.vnpay.tmn-code=${VNPAY_TMN_CODE:}
app.vnpay.hash-secret=${VNPAY_HASH_SECRET:}
app.vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:8080/payment/vnpay/return}
